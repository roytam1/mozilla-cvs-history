DEPTH		= ../../..
topsrcdir	= @top_srcdir@
srcdir		= @srcdir@
VPATH		= @srcdir@

include $(DEPTH)/config/autoconf.mk

include $(topsrcdir)/config/rules.mk

FILES := \
	sidebar.rdf \
	accounts.rdf \
	contacts.rdf \
	calls.rdf \
	prefs.rdf \
	$(NULL)

FILES := $(addprefix $(srcdir)/, $(FILES))

libs::
	$(INSTALL) $(FILES) $(DIST)/bin/defaults/profile

install::
	$(SYSINSTALL) $(IFLAGS1) $(FILES) $(DESTDIR)$(mozappdir)/defaults/profile
